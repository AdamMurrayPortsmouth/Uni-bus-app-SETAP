import 'feature.dart';

/// Holds bus stop information.
class BusStop extends Feature
{
  /// The constructor assigning the id, name, longitude and latitude.
  BusStop(super. id, super.name, super.long, super.lat);
}